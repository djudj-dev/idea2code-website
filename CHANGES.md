# UI Components — Changes to sync back to registry

## button.tsx

- `rounded-lg` → `rounded-full` (base + all size variants including button-group overrides)
- Added `cursor-pointer` to base classes
- Increased horizontal padding: default `px-2.5` → `px-4`, lg `px-2.5` → `px-5` (+ matching icon pr/pl)
